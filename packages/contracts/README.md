# @funtog/contracts

The frozen contract surface. Every subsystem codes against these types and the service
interfaces in `services.ts`. This package is shared and changes only via a contract-change
proposal (see root `CONTRIBUTING.md`). It is the single source of truth for the §7 contract
matrix in `docs/architecture/funtog-architecture.md`.
