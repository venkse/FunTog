{
  "name": "funtog",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["packages/*", "services/*", "apps/*"],
  "scripts": {
    "build": "tsc -b",
    "test": "npm test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.16.0"
  }
}
